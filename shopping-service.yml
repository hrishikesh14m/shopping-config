server:
  port: 8088

spring:
  application:
    name: shopping-service

  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka
      register-with-eureka: true
      fetch-registry: true

