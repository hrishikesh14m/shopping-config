server:
  port: 8083

spring:
  application:
    name: cart-service

  eureka:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

